module BetsHelper
  # def link_to_add_bet(game_line = @game_line, team = @team)
  #   link_to 'Bet on this team',
  #           bets_path(bet: { game_line_id: game_line.id,
  #                            team_id: team.id }),
  #           method: :post, remote: true, class: 'btn btn-sm btn-primary'
  # end
end
